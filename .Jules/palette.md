# UX Journal

## Critical Learnings

- Background zoom/scroll in ShaderGradient can be disabled by setting `pointer-events: none` on the Canvas component, allowing events to bubble up to parent handlers.
